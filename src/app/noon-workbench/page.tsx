import NoonWorkbenchWorkspace from "./noon-workbench-workspace";

export default function NoonWorkbenchPage() {
  return (
    <main className="page-shell product-page-shell repository-page-shell noon-workbench-page">
      <NoonWorkbenchWorkspace />
    </main>
  );
}
