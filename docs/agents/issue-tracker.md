# Issue tracker

This repo tracks issues locally as markdown files under `.scratch/`.

Use this workflow when creating or updating issues:

- Store issue files under `.scratch/<feature-or-topic>/`.
- Use one markdown file per issue.
- Keep issue titles, status, acceptance criteria, and implementation notes in the file.
- Do not assume a remote issue tracker unless the repo configuration is changed later.
