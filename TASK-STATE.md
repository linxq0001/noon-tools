# TASK STATE

- 当前任务：将 Noon 工作台分页器改为与 repositories 页面完全一致的共享分页 UI，并保持服务端分页与请求竞态保护。
- 当前事实来源：`docs/superpowers/specs/2026-07-22-noon-workbench-repository-pager-design.md`、`docs/superpowers/plans/2026-07-22-noon-workbench-repository-pager.md`。底层同步事实继续引用 2026-07-12 双同步设计与计划。
- 已完成：内部 API 同步库、CloakBrowser 启动脚本、任务来源路由、工作台双同步入口已存在于工作区；第一轮审查的两个 HIGH 已修复（仓库旧入口显式来源、Offer 完整请求体）。
- 验证摘要（2026-07-21，修复后重跑）：聚焦测试 29/29 通过；`npx tsc --noEmit` 退出 0；`npm run build:next` 退出 0。构建提示多锁文件 workspace root 推断警告。
- 用户决定（2026-07-21）：现阶段 FBN/FBP 模式禁用导出同步；后续有需要再开发真实 FBN/FBP 导出，并保持现有 `source=export` 请求结构兼容。
- 已完成：FBN/FBP 下导出按钮禁用并提供说明；服务端拒绝 `export + fbn`，保留未来开放同一请求组合的兼容边界。
- 用户决定（2026-07-21）：应用仅限本机单用户；采用 `127.0.0.1` 绑定与 loopback Host/Origin 校验，不引入账户系统。
- 已完成：两处导出按钮共用 FBN/FBP 禁用条件和可访问说明；同步敏感路由接入本地访问守卫；内部 API 子进程不再读取或注入 Token。
- 验证摘要（2026-07-21，最终修复后）：串行聚焦测试 35/35 通过；`npx tsc --noEmit` 退出 0；`npm run build:next` 退出 0。并行 Node 25 runner 曾出现一次 IPC deserialize 错误（无断言失败），串行重跑完整通过。构建仍提示多锁文件 workspace root 推断警告。
- 差异检查：`git diff --check` 唯一告警来自任务范围外既有改动 `scripts/upload-noon.js` 文件末尾空行，未修改。
- 审查摘要（2026-07-21）：最终全栈定向复核与 UI 复核均无 BLOCKER/HIGH；此前移动端问题已关闭。
- UI 一致性摘要（2026-07-21，完成）：已提取共享同步操作组并集中普通/导出禁用条件；桌面顶部靠右、空状态居中；375px 实测 `scrollWidth=clientWidth=375`。最终重跑串行聚焦测试 35/35、TypeScript、Next build 通过；最终审查问题已关闭。
- 调试摘要（2026-07-21）：稳定复现 `Origin: http://127.0.0.1:3000` 被 403 拒绝，而 `localhost` 可进入业务层。根因是 Next 将 `request.url` 主机规范化为 `localhost`，守卫却用它与浏览器 Origin 比较。已新增红绿回归测试，并改为按实际 `Host` 构造期望 Origin；修复后同一 127 请求进入业务校验。
- 来源守卫验证（2026-07-21）：定向测试 12/12 通过；`npx tsc --noEmit` 退出 0；`npm run build:next` 退出 0；真实 127 POST 已从 403 进入业务校验。两阶段只读审查无 BLOCKER/HIGH/MEDIUM/LOW，loopback、协议、Host 与端口限制保持有效。构建仍仅有既知多锁文件 workspace root 警告。
- 运行时恢复（2026-07-21）：生产构建与运行中的开发服务器共用 `.next`，导致开发进程引用缺失 chunk 并返回 HTTP 500。已确认进程属于本项目后重启 `npm run dev:next`，记录 PID 97239/端口 3000；浏览器复验标题为 `Noon Tools`、无 Runtime Error、工作台正常加载。未修改业务代码。
- Noon Offer 400 调试（2026-07-21）：真实单页请求返回 `filters: Input should be a valid dictionary`；同一店铺改为 `filters: {}` 后 HTTP 200，`total=109337`、第一页 100 条。已按红绿测试将请求契约从空数组修正为空对象，并同步更新双同步设计事实。
- Noon Offer 400 验证（2026-07-21）：聚焦测试 22/22 通过，`npx tsc --noEmit` 退出 0；真实第二页同样 HTTP 200、总数 109337、返回 100 条且含可映射 SKU。未启动 109337 条完整同步。两阶段只读审查无 BLOCKER/HIGH/MEDIUM/LOW；分页、8 并发、动态店铺代码及凭据边界未受影响。为避免再次破坏运行中的 `.next`，本轮未并行执行 Next build；上一轮生产构建已通过。
- 分页窗口调试（2026-07-21）：真实响应显示第 1/98/99 页均为 `total=109337` 且 100 条，第 100/101 页固定为 `total=0`、0 条；这不是目录动态变化，而是旧列表接口约 9900 条的硬窗口。已撤销会静默写入残缺快照的动态总数策略，恢复总数一致性保护，并为第 100 页窗口返回“请使用导出同步”的明确错误。
- 限流调试（2026-07-21）：动态总数修复后的完整复验读取到第 81 页时遇到 HTTP 429；短时间连续真实验证触发 Noon 限流。已新增红绿测试，仅对 HTTP 429 最多尝试 5 次，按 1/2/4/8 秒指数退避；其他错误仍立即失败。
- 导出同步调试（2026-07-21）：Noon 导出任务 `EXP4Q7MALZYK` 创建、轮询和下载成功，但旧解析器写出了 `rowCount=0` 空快照。已新增红绿保护：0 行不得写成功快照，并报告安全的 Content-Type、字节数和文件签名用于定位格式，不输出文件内容或凭据。
- 导出参数证据（2026-07-21）：分别以 `country=sa` 和 `country=ae` 创建真实导出，均下载到 228 字节、签名 `psku_cod...` 的仅表头 CSV，说明当前 `{ country, noon_status: "all" }` 参数组合不适用。两份空/残缺成功快照均已移入 `exports/noon-catalog-sync/incomplete/`，不会被页面读取。下一步需使用现有店铺 APIJWT 只读调用官方 `GET /impex/v1/export/category/list`，读取 `noon_catalog_globalcatalogexport` 的动态参数元数据；该操作会读取真实店铺凭据，需用户单独确认，输出不包含凭据。
- 官方导出契约（2026-07-21）：经用户单次确认读取官方分类元数据，并从当前 Noon 前端契约确认 `noon_status` 的有效值为 `live`、`not_live`、`no_global_offer`。外部 `noonStatus=all` 现保持兼容，内部顺序创建三份导出并合并去重。
- 大文件下载调试（2026-07-21）：真实三状态任务中 `live`、`not_live` 已完成，`no_global_offer` 在下载阶段超过 6 分钟无连接和进度，已通过本地任务取消接口停止子进程。新增分块读取、每 5 MB 进度日志及 60 秒无数据超时；红绿测试覆盖正常流与停滞流，API 导出测试 11/11 通过。
- 下载连接补充定位（2026-07-21）：首次复验显示停滞发生在下载请求等待响应头、早于响应体读取，现追加 AbortController 保护，等待响应头和读取响应体均有 60 秒无进展超时；新增回归测试后 API 导出测试 12/12、相关聚焦测试 39/39、`npx tsc --noEmit` 均通过。
- 真实单分区复验（2026-07-21）：创建 `no_global_offer` 导出 `EXP4MHI7BTQY` 后持续约 15 分钟收到 Noon PENDING 状态，未进入下载阶段；本地轮询已停止，Noon 端报表未删除。该结果不能证明真实大文件下载完成，也未写任何快照。
- 最终审查修复（2026-07-21）：按审查意见改为使用 PSKU/SKU 组合去重，并拒绝列数异常、引号未闭合或同时缺少 PSKU/SKU 的数据行；非 Web Stream 的 `arrayBuffer` 兼容路径也纳入空闲超时并在超时后取消/销毁响应体，非成功下载响应主动取消响应体。对应导出测试增至 16/16，`npx tsc --noEmit` 通过。
- 页面崩溃证据（2026-07-22）：当前无运行中的同步/上传/浏览器重任务，Next CPU 连续采样为 0%、无 Swap；最新 PRJ517205 Global 快照 78 MB/109337 行，`/api/noon-catalog-sync` 单次响应约 148 MB、1.8-2.2 秒，前端随后一次渲染全部行，Chrome 标签页出现错误码 5。开发服务器运行记录 PID 已过期，但不是崩溃根因。
- 页面崩溃修复（2026-07-22）：目录读取接口新增服务端分页，默认 50、最大 100 行，返回 `page/pageSize/totalItems/totalPages`；工作台显示真实总数并提供上一页/下一页。真实第一页/第二页响应由约 148 MB 降至 18-19 KB，每页 50 行，总数 109337、总页数 2187。
- 验证摘要（2026-07-22）：分页红绿测试与相关前端、读取、批量操作、安全回归 27/27 通过，`npx tsc --noEmit` 退出 0；浏览器加载稳定，点击下一页后显示第 2/2187 页且 DOM 仅 50 个商品 article，控制台无错误。为避免运行中的开发服务器与构建共用 `.next` 再次冲突，本轮未执行 Next build。
- 审查修复（2026-07-22）：目录请求新增 AbortController，仅最新请求可提交状态；请求期间清空旧选择并禁用翻页、选择与批量写操作，服务端钳制页码会回写客户端；无快照响应保持规范化后的请求 pageSize。相关回归增至 28/28，TypeScript 通过，浏览器重载及第 2 页复验仍为 50 行、无控制台错误。
- 最终审查（2026-07-22）：补充加载期间禁用每行复选框，并在批量操作入口拒绝加载态；最终独立审查 BLOCKER/HIGH/MEDIUM/LOW 均为 0。
- 分页 UI 一致性（2026-07-22）：已从 repositories 提取共享 `Pager`，两页统一使用总数、前后箭头、页码窗口、省略号、10/20/50 条每页及指定页跳转；Noon 默认仍为 50 条/页，切换每页条数会回到第 1 页，请求期间统一禁用分页控件。
- 分页 UI 验证（2026-07-22）：全量 `npm test -- --test-isolation=none` 245/245 通过，`npx tsc --noEmit` 退出 0；浏览器实测 20 条/页仅渲染 20 个 article，跳至第 8 页后显示 `1 … 7 8 9 … 5467`，控制台错误为 0。
- 分页 UI 审查（2026-07-22）：共享 Pager 已补齐分页导航、前后页、每页条数、当前页与省略号的可访问语义；修复后全量测试仍为 245/245，TypeScript 通过。审查另识别 repositories 原有请求缺少取消/最新响应保护，属于本次 UI 改造前已存在的独立 MEDIUM，未扩展本次范围。
- 商品行布局修复（2026-07-22）：商品标题限制为两行省略，PSKU/SKU 单行省略，商品与各业务列统一设置最小宽度和溢出边界；图片采用懒加载，有 URL 加载失败时回退“无图”。当前 PRJ517205 Global 最新 109337 行快照的 `imageUrl` 全为空，因此浏览器中的 50 条当前页均显示占位，无法从页面层恢复真实图片。
- 商品行验证（2026-07-22）：全量测试 246/246、`npx tsc --noEmit` 和差异检查通过；浏览器测得商品信息列 right=564、售价列 left=576，无覆盖，标题为两行高度，PSKU/SKU 为 ellipsis，控制台错误为 0。
- 未完成：真实登录店铺的受限两页验证仍因登录态失效未覆盖。
- 失败证据（2026-07-21）：受限两页只读验证在 Catalog 导航后被重定向至登录页，报错“测试店铺登录态已失效”；未发出 Offer 分页请求、未写快照。CLI 没有受限页数参数，未执行十万级完整同步。
- 工作区：`codex/nextjs-phase-1-settings`，存在多轮未提交改动及用户已有文件删除；不得重置或清理无关变化。
- 下一步：完成本轮分页 UI 只读审查；之后待 Noon 的 `no_global_offer` 报表可完成时再次验证真实下载。开发服务器 PID 记录过期需在下次安全重启时更新。
- 完成条件：本地验证保持通过，无 BLOCKER/HIGH 审查问题；明确记录真实只读验证覆盖或未执行原因；交付前删除本文件。
